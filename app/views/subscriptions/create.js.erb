(function(){
  var questionSelector = "*[data-resource='question'][data-id='<%= @subscription.question.id %>']",
      $question = $( questionSelector );

  $question.find('.subscriptions').html('<%= j(render "subscriptions/new", question: @subscription.question) %>')

}).call(this);