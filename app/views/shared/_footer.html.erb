<% if user_signed_in? %>
  <ul id='question-queries'>

  </ul>
  
  <footer>
  <!-- new question -->
  <%= render 'questions/form', question: current_user.questions.new, html: { } %>
  </footer>
<% end %>